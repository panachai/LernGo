settings:
  legacy:
    force: false
    interval: 0s
  # resources:
  #   outputs: outputs.log
  #   logs: logs.log
  #   errors: errors.log
schema:
- name: LernGo
  path: B:\Personal\LernGo
  commands:
    # vet:
    #   status: true
    fmt:
      status: true
    # generate:
    #     status: true
    build:
      status: true
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
